# PC 없이 돌리기 — GitHub 무료 서버로 옮기기

PC를 꺼도 30분마다 알아서 뉴스를 모으고, 폰이든 회사 PC든 주소만 치면 보이게 만듭니다.
**돈은 들지 않습니다.**

한 번만 설정하면 그 뒤로는 손댈 일이 없습니다. 15~20분 정도 걸립니다.

---

## 먼저 알아두실 것

| | PC에서 돌릴 때 (지금) | GitHub로 옮긴 뒤 |
|---|---|---|
| PC | 켜져 있어야 함 | **꺼도 됨** |
| 뉴스 갱신 | RUN.bat 돌릴 때 | **30분마다 자동** (평일 장중) |
| 시세 갱신 | 30초마다 | 30분마다 |
| 보는 곳 | 그 PC에서만 | **폰·회사PC 어디서나** |
| 속보 알림 | 윈도우 알림 | 텔레그램 |

시세가 30초 → 30분으로 느려집니다. 무료로 쓰는 대신 치르는 값입니다.
**호가 보고 매매하는 용도로는 못 씁니다.** "지금 어느 테마가 뜨는지" 보는 용도입니다.

주식 시세를 초 단위로 보셔야 하면, 그건 PC에서 `RUN.bat`을 돌리는 게 맞습니다.
둘 다 쓰셔도 됩니다 — 서로 방해하지 않습니다.

---

## 1단계 — GitHub 계정 만들기

이미 있으시면 넘어가세요.

1. https://github.com 접속
2. 오른쪽 위 **Sign up**
3. 메일 주소, 비밀번호, 사용자 이름(영문) 입력
4. 메일로 온 인증 코드 입력

> 사용자 이름은 나중에 주소에 그대로 들어갑니다.
> 예를 들어 `hyunjeong`으로 만들면 주소가 `hyunjeong.github.io/...` 가 됩니다.

---

## 2단계 — 저장소 만들기

1. 로그인 후 오른쪽 위 **+** → **New repository**
2. **Repository name**: `theme-radar` (다른 이름도 됩니다, 영문으로)
3. **Public** 선택 ← 중요

   > Private으로 하면 웹페이지 기능이 유료($4/월)입니다.
   > Public이면 주소를 아는 사람은 대시보드를 볼 수 있습니다.
   > 뉴스와 공개 시세만 있어서 민감한 내용은 없습니다.
   > 텔레그램 토큰은 4단계에서 따로 넣으므로 공개되지 않습니다.

4. 나머지는 건드리지 말고 **Create repository**

---

## 3단계 — 파일 올리기

1. 만들어진 저장소 화면에서 **uploading an existing file** 링크 클릭
   (안 보이면 **Add file** → **Upload files**)

2. 탐색기에서 `theme-news-app` 폴더를 열고 **Ctrl+A**로 전부 선택한 뒤,
   브라우저 창으로 **드래그해서 놓으세요.**

   ⚠️ 폴더째로 끌지 말고, **폴더를 열어서 안의 내용물을 전부** 끌어야 합니다.

3. 아래 초록색 **Commit changes** 버튼 클릭

---

## 3-1단계 — 자동 갱신 파일 만들기 ★ 이게 핵심입니다

30분마다 자동으로 돌게 만드는 파일입니다. **이것만 빠지면 아무것도 안 돌아갑니다.**
GitHub 웹에서 직접 만드는 게 확실합니다.

1. 저장소 화면에서 **Add file** → **Create new file**

2. 파일 이름 칸에 아래를 **그대로** 입력하세요. 슬래시(/)까지 똑같이:

   ```
   .github/workflows/update.yml
   ```

   > 슬래시를 칠 때마다 폴더가 자동으로 만들어집니다. 정상입니다.

3. 바탕화면 `theme-news-app` 폴더에서 **`github_workflow_paste_me.txt`** 를 메모장으로 엽니다.
   안내선 안쪽 내용을 **전부 선택(Ctrl+A 후 안내문 부분만 빼고) → 복사**하세요.

   > 파일 맨 위 안내문(`=====` 로 둘러싸인 부분)은 빼고,
   > **`name: 테마 레이더 자동 갱신`** 부터 끝까지만 복사하시면 됩니다.

4. GitHub 화면의 큰 입력칸에 붙여넣고, 아래 **Commit changes** 클릭

5. 저장소 첫 화면에 `.github` 폴더가 생겼는지 확인

---

## 4단계 — 웹페이지 켜기

1. 저장소 위쪽 **Settings** 탭
2. 왼쪽 메뉴에서 **Pages**
3. **Source** 를 **GitHub Actions** 로 바꾸기

   > 기본값인 "Deploy from a branch"가 아니라 **GitHub Actions** 입니다.

---

## 5단계 — 첫 실행

1. 저장소 위쪽 **Actions** 탭
2. 노란 안내가 뜨면 **I understand my workflows, go ahead and enable them** 클릭
3. 왼쪽에서 **테마 레이더 자동 갱신** 클릭
4. 오른쪽 **Run workflow** → 초록 **Run workflow** 버튼

3~5분 기다리면 초록 체크(✓)가 뜹니다.

**주소는 여기입니다:**

```
https://<사용자이름>.github.io/theme-radar/
```

Settings → Pages 화면 맨 위에도 주소가 표시됩니다. 폰 홈 화면에 추가해 두시면 편합니다.

---

## 6단계 (선택) — 텔레그램 속보 알림

PC가 꺼져 있어도 속보를 폰으로 받습니다. 필요 없으면 건너뛰세요.

**먼저 텔레그램에서:**

1. 텔레그램 앱에서 `@BotFather` 검색 → 대화 시작
2. `/newbot` 입력 → 봇 이름과 아이디(끝이 `bot`이어야 함) 입력
3. `123456789:AAxxxxxx...` 같은 **토큰**을 알려줍니다. 복사해 두세요.
4. 방금 만든 봇을 검색해서 대화 시작 → 아무 말이나 한 마디 보내기
5. 브라우저 주소창에 아래를 치고 접속 (`<토큰>` 자리에 3번 값):
   ```
   https://api.telegram.org/bot<토큰>/getUpdates
   ```
6. 나온 내용에서 `"chat":{"id":123456789` 의 **숫자**를 복사

**GitHub에서:**

7. 저장소 **Settings** → 왼쪽 **Secrets and variables** → **Actions**
8. **New repository secret** 을 눌러 두 개를 등록합니다.

   | Name | Secret |
   |---|---|
   | `TELEGRAM_TOKEN` | 3번에서 받은 토큰 |
   | `TELEGRAM_CHAT_ID` | 6번의 숫자 |

Secrets에 넣은 값은 Public 저장소에서도 남에게 보이지 않고, 실행 기록에도 가려져 나옵니다.

슬랙을 쓰신다면 `SLACK_WEBHOOK` 하나만 등록하시면 됩니다.

---

## 알아두면 좋은 것

**갱신 시각은 조금씩 밀립니다.** GitHub 무료 서버는 일이 몰리면 5~20분 늦게 시작합니다.
30분마다라고 해도 실제로는 35분, 45분 간격이 될 수 있습니다. 정상입니다.

**60일 동안 저장소를 건드리지 않으면 자동 실행이 꺼집니다.** GitHub 정책입니다.
꺼지기 전에 메일이 오고, Actions 탭에서 버튼 한 번 누르면 다시 켜집니다.
가끔 들어가서 아무 파일이나 살짝 고쳐 두시면 계속 돌아갑니다.

**테마 사전을 고치고 싶을 때** — 저장소에서 `themes.json` 을 열고 연필 아이콘(✏️)을 눌러
바로 고친 뒤 Commit 하면, 다음 실행부터 반영됩니다. PC에서 파일을 다시 올릴 필요 없습니다.

**수집이 실패해도 화면은 안 비워집니다.** 직전 데이터를 그대로 두고, 화면 상단 시각으로
"언제 기준인지" 알 수 있게 해 뒀습니다.

**잘 안 될 때** — Actions 탭에서 빨간 ✗ 가 붙은 실행을 클릭하면 어디서 멈췄는지 로그가 나옵니다.
그 화면을 그대로 보내주시면 봐 드리겠습니다.

---

> 자동 분류 결과이며 투자 판단의 근거가 아닙니다.
